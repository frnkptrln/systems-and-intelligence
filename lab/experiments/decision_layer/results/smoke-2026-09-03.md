# Smoke record — 2026-09-03

**What ran.** A design probe (the construction computed by hand and by script on `K1`, `K2`, `K3`, `K4` at costs 0–4 under all four cards) and, after the module was finished, one smoke run of the CI subgrid and one of the full grid (172 classes × 4 cards × 5 costs = 3,440 cells), both before `README.md` was committed.

**What was seen.** Every prediction the README declares: the `K1|D1|3` table (VoI 0 against 5, regret radius 10 against 0), `K4|D1|3` and `K1|D2|3` strict, `K4|U` never strict with maximal VoI (2^k − 1)/256, the risk-inversion pair in `K1|D1`, the Case A/B radii 0 and 10, `K2` never strict, and the coverage criterion equal to the enumeration on all 850 (cube, `D1`, cost) cells. Strict cells in the full grid: 7 under `D1` (4 at cost 2, 3 at cost 3), the same 7 under `D2`, none under `U` or `D3`. Timings: CI subgrid 9.2 s, full grid 28.7 s on one CPU.

**What was not seen.** Nothing was withheld; the smoke run computed the full grid. The results commit reproduces it from the committed module.

**Reproduction.** `results/ci_subgrid.json`, written by `--save`, is the CI subgrid recomputed; `tests/test_decision_layer.py` pins it and the P1–P5 quantities directly.
